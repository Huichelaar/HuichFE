Uncompressed frame data handler:

8059b08 is a branch to 80d1694 (uncompress lz77 - 8bit)
  r0 is frame data ptr
  r1 is destination

change this to a branch to my own location
  check if highest bit set in r0
    if no:
      bl 80d1694, return via r1
    if yes:
      mov r3,r0 @save the offset
      ldr r0,[r6,#0xc] @section data
      add r0, #0x2c @end of section data
      ldr r0,[r0] @bytes to copy
      lsr r0,#5 @8-word blocks to copy using cpufastset
      mov r2,r0
      mov r0,r3 @get the params in order
      bl 80d1674 @write the fast part
      @now r0 is last location and r1 is last destination, time to memcpy
      mov r3, #0
      mvn r3, r3 @r3 is now 0xFFFFFFFF
      LoopStart:
        ldmia r2! r0 @loads word from r0
        cmp r2,r3
        beq End
        stmia r2! r1 @store to r1
      End:
      pop and return via r1

